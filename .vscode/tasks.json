{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "flash",
            "type": "shell",
            "command": "cd src && make flash"
        },
        {
            "label": "monitor",
            "type": "shell",
            "command": "cd src && make monitor",
            "problemMatcher": []
        }
    ]
}